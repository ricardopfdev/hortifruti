server.port=${PORT:8080}

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.thymeleaf.cache=true

# Conexão Postgres: resolvida em RenderEnvironmentPostProcessor (perfil prod)
